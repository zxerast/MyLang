# Требования к компиляции

1. cmake -S . -B build -G Ninja  -  для корректной работы с модулями .cppm
2. libclang-dev  -  для работы C импортов
3. /runtime с .o файлами в корне проекта  -  из-за линковки с .asm функциями
4. Linux x86-64
5. CMake >= 3.28
6. 'readline' и 'ncurses' для линковки 
7. 'nasm', 'ld', 'gcc'
8. Запуск из корня проекта