export namespace System {
    export namespace Data {
        export type Cube = int[][][];
        export type HyperCube = int[][][][];

        export struct Config {
            int depth = 1;
            float threshold = 0.5;
            string tag = "STABLE";
        }
    }
}

// Изменяем глобальные значения по умолчанию для структуры до её инстанцирования
// Это должно повлиять на все будущие Config в программе
export void SetupConfig() {
    System::Data::Config.depth = 42;
    System::Data::Config.tag = "BOSS_READY";
}