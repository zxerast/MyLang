class Number{
    int x;
    int y;

    void Add(int a, int b){
        x = a;
        y = b;
    }
}

int main(){
    Number f = new Number();

    f.Add;  //  Так вызывать метод с параметрами нельзя, а компилятор не жалуется

    int i = 0;
    auto[] arr1; //  Не работает
    Number[] arr2; // Не работает

    while(i < 10){
        Number g = new Number();
        push(arr, g);
        i++;
    }

    arr[3].Add(3, 4);   //  Тоже не будут работать
    print(arr[3].x, arr[3].y);

    return 0;
}