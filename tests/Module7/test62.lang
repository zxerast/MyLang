int main() {
    int a = sum();
    int b = sum(10);
    int c = sum(10, 20);
    int d = sum(10, 20, 30);

    print("TEST 2 OK", a, b, c, d);

    return a + b + c + d;
}

int sum(int x, int y, int z = 5) {
    return x + y + z;
}

//  Status: OK
